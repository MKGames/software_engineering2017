<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :decription %>
    <%= form.text_area :decription, id: :order_decription %>
  </div>

  <div class="field">
    <%= form.label :id_user %>
    <%= form.number_field :id_user, id: :order_id_user %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
